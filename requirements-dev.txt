pytest>=8.0
pytest-asyncio>=0.24
# Pinned, because the floor was choosing versions by accident. 0.13.316 is
# the newest release still declaring Requires-Python >=3.13; 0.13.317 and later
# need 3.14. CI runs 3.13, so pip stopped there — and *that* is what selected
# homeassistant 2026.2.3, pytest 9.0.0 and pytest-asyncio 1.3.0, all of which
# this package hard-pins underneath. The day a 3.13-compatible successor lands,
# all four move at once and nobody decided it.
#
# Raise this deliberately, and read the CI count afterwards.
#
# There is a second pin, in .github/workflows/validate.yml: the `tests-floor`
# job installs 0.13.294, which is homeassistant 2025.11.0, the floor hacs.json
# declares. That one moves when the declared floor moves. Two pins, because a
# test cannot look up which Home Assistant a harness version carries.
pytest-homeassistant-custom-component==0.13.316
pytest-xdist>=3.6
ruff>=0.9
